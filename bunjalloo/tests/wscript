#!/usr/bin/env python
# encoding: utf-8
import os

def build(bld):
  #
  obj = bld.create_obj('cpp', 'staticlib')
  obj.env = bld.env('sdl').copy()
  obj.source = '''Main.cpp'''
  obj.target = '''cppunitmain'''

  # build subdirs
  bld.add_subdirs('patch-test gui-test lang-test images sprite bwtimage')
  #here = os.path.dirname(bld.m_curdirnode.find_source(obj.source).abspath(obj.env))
  #[bld.add_subdirs(d) for d in os.listdir(here) if os.path.isdir(os.path.join(here,d))]
